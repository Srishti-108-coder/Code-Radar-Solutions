#include <stdio.h>
int main()
{
    int n,x,count=0;
    scanf("%d",&a);
    for(x=1;x<=n;x++)
    {
    if(n%x==0)
    count++;
    }
    if(count==2)
    printf("Prime");
    else
    printf("Not Prime");

    return 0;

    
}